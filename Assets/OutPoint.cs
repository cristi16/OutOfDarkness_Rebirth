using UnityEngine;
using System.Collections;

public class OutPoint : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//transform.position = new Vector3(transform.position.x,GameObject.FindGameObjectWithTag("Kid").transform.position.y+0.2f,transform.position.z);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
